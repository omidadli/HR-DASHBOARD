# پرامپت ساخت «دستیار هوشمند غربالگری رزومه سیلانه سبز» — برای ارائه به AI Studio

> این سند را به‌صورت کامل و یکجا در AI Studio (با همین پروژه ضمیمه‌شده) پیست کن.
> نقش AI Studio هنگام اجرا: **مدیر محصول + معمار ارشد + مهندس هوش مصنوعی + طراح UI/UX** به‌صورت هم‌زمان.

---

## بخش ۰ — دستور کلی به AI Studio

تو قرار است یک اپلیکیشن وب **فارسی، راست‌چین (RTL) و تک‌کاربره** بسازی به نام **«دستیار هوشمند غربالگری رزومه سیلانه سبز»**. این اپلیکیشن فقط و فقط یک کار دارد:

> کارشناس منابع انسانی هلدینگ سیلانه سبز برای یک دپارتمان، چند رزومه را بارگذاری می‌کند، به **چند سوال خیلی ساده با چک‌باکس** جواب می‌دهد، هوش مصنوعی همه رزومه‌ها را تحلیل می‌کند و آن‌ها را در **سه دسته «مصاحبه شود / بررسی شود / رد شود»** اولویت‌بندی و چیدمان می‌کند. رزومه‌های منتخب قابل افزودن به **«بانک رزومه»** هستند که بر اساس دپارتمان گروه‌بندی و قابل فیلتر است.

سه اصل مقدس این محصول (هیچ‌وقت نباید قربانی شوند):

1. **سادگی مطلق:** کار با اپ بدون هیچ آموزشی، بدون فرم طولانی، بدون اصطلاح فنی، حداکثر با ۳ کلیک به نتیجه برسد. در هر صفحه دقیقاً یک اقدام اصلی وجود دارد.
2. **UI/UX فوق‌العاده دوست‌داشتنی:** تمیز، مدرن، منظم، رنگ‌بندی معنایی، انیمیشن‌های نرم، فونت خوانا، خالی از شلوغی و حس یک محصول «سری و حرفه‌ای».
3. **تحلیل AI حرفه‌ای، دقیق و مستدل:** هیچ امتیاز تصادفی، هیچ داده ساختگی، هیچ تحلیلی بدون شاهد از متن رزومه. هر امتیاز باید با نقل‌قول واقعی از رزومه پشتیبانی شود.

قبل از کدنویسی: (الف) یک بازبینی کوتاه از وضعیت فعلی بده، (ب) برنامه فایل‌ها (keep/modify/create/delete) را فهرست کن، (ج) سپس قدم‌به‌قدم پیاده‌سازی کن و در پایان همه جریان‌ها را در مرورگر تست کن و خروجی `npm run lint` را پاک‌سازی کن.

---

## بخش ۱ — وضعیت کد فعلی و نقطه شروع

پروژه فعلاً دو لایه دارد که با هم قاطی شده‌اند:

**لایه الف) یک ویزارد ۳ مرحله‌ای غربالگری (خوب و قابل‌استفاده — همین الان در `src/App.tsx` فعال است):**
- `src/App.tsx`، `src/components/StartScreen.tsx`، `ProcessingScreen.tsx`، `ResultsScreen.tsx`، `CandidateCard.tsx`، `UnjudgeableSection.tsx`، `StepperHeader.tsx`
- `src/lib/extractText.ts` (استخراج واقعی متن PDF دومستونی با مرتب‌سازی مختصاتی، DOCX با mammoth، ZIP با JSZip، TXT)
- `src/lib/queue.ts` (صف پردازش با همزمانی ۲، ریتری، abort، پیشرفت لحظه‌ای)
- `server/screening-gemini.ts` (سه پاس: درک شغل، ارزیابی هر رزومه با شواهد و قوانین سقفی، کالیبراسیون + کلاینت Gemini با circuit breaker و fallback)
- `src/types/screening.ts`، `src/lib/normalizeFa.ts`، `src/lib/excelExport.ts`
- `server/tehran-time.ts` (تاریخ جلالی واقعی)، توکن‌های طراحی در `src/index.css` (Vazirmatn، RTL، رنگ‌های semantic با کنتراست AA)

**لایه ب) یک HRMS غول‌پیکر بی‌ربط (باید کامل حذف شود):**
حقوق و دستمزد، مرخصی و تردد، پرونده پرسنلی، چارت سازمانی، OKR، آموزش، چک‌لیست، داشبورد تحلیلی، پوسته موبایل، دستیار صوتی، تابلو کانبان، تولید آگهی، و اینتگریشن‌های ساختگی رقبا (HireVue / Eightfold / ZipRecruiter)، نقش‌های کاربری و RBAC.

### دستورات صریح درباره کد

**حذف کن (و هیچ معادلشان را نساز):**
- کل پوشه‌های `src/components/{recruitment,employees,attendance,payroll,performance,training,checklists,analytics,dashboard,mobile,ai-governance}` به‌جز آنچه در بالا به‌عنوان هسته غربالگری آمده
- کامپوننت‌های `Sidebar`، `CommandPalette`، `FloatingQuickActions` و سوییچر نقش
- `server/{store.ts,payroll-service.ts,leave-service.ts,statutory.ts,employeeRepo.ts}` و تمام endpointهای مرتبط در `server.ts` (حقوق، مرخصی، کارمند، اتوماسیون، رقبا، نقش‌ها)
- فایل Prisma (یا به‌عنوان مرجع رهاشده باقی بماند؛ هیچ مهاجرت/دیتابیسی اضافه نکن)

**حفظ و ارتقا بده:** موتور استخراج متن، صف پردازش، سه پاس AI و circuit breaker، توکن‌های طراحی، توابع تبدیل تاریخ جلالی و نرمال‌سازی فارسی، درایور اکسل.

**می‌سازی (مهم‌ترین کارها):**
1. پرسش‌نامه هوشمند چک‌باکسی (الان اصلاً وجود ندارد؛ الان فقط یک textarea شرح شغل هست).
2. نتایج سه‌دسته‌ای واقعی به‌جای یک لیست بلند واحد، با صفحه‌بندی ۱۰‌تایی.
3. کارت رزومه با ۴ دکمه اقدام و دراور جزئیات تحلیل.
4. تب «بانک رزومه» با گرید دپارتمان‌ها، صفحه دپارتمان و فیلترها.
5. ماندگاری داده (batchها و بانک) روی فایل JSON سمت سرور.

---

## بخش ۲ — دامنه محصول (Out of Scope — صریحاً نساز)

- ❌ احراز هویت، نقش کاربری، چندکاربره بودن، سوییچ نقش
- ❌ حقوق، تردد، مرخصی، مالیات، فیش، چارت سازمانی، OKR، آموزش
- ❌ تابلو کانبان/قیف استخدام، مراحل استخدام، آفر، تقویم مصاحبه
- ❌ تولید آگهی شغلی، چت‌بات، دستیار صوتی، تماس تصویری
- ❌ هرگونه یکپارچه‌سازی واقعی با سرویس پیامک/ایمیل/واتساپ/ATS خارجی (فقط لینک آماده و کپی متن — بخش ۷)
- ❌ نمودار و داشبورد تحلیلی، گزارش اکسل پیچیده (یک خروجی ساده اکسل از نتایج کافی است)
- ❌ اعداد، رزومه، نام یا امتیاز ساختگی/تصادفی در هیچ جای محصول

---

## بخش ۳ — معماری اطلاعات و ناوبری

استک بدون تغییر بماند: **React 19 + Vite + TypeScript + Tailwind v4 + Express (tsx) + @google/genai**؛ آیکون‌ها `lucide-react`، انیمیشن `motion`، فونت Vazirmatn. کلید `GEMINI_API_KEY` فقط سمت سرور خوانده شود.

اپ دقیقاً **دو تب اصلی** دارد (تب‌بار ساده در بالا، روی موبایل تمام‌عرض، با لیبل و آیکون):

| تب | آیکون پیشنهادی | محتوا |
|---|---|---|
| **غربالگری جدید** | `Sparkles` / `ScanSearch` | ویزارد ۳ گامی + لیست «غربالگری‌های اخیر» |
| **بانک رزومه** | `Library` / `Bookmark` | گرید دپارتمان‌ها → صفحه رزومه‌های آن دپارتمان با فیلتر |

هدر ثابت: لوگوی متنی ساده «سیلانه سبز | دستیار غربالگری رزومه» + سمت راست تب‌ها. هیچ منوی دیگری نباشد.

### ویزارد غربالگری (۳ گام با Stepper بالای صفحه)

**گام ۱ — دپارتمان، سوال‌ها و رزومه‌ها (یک صفحه واحد، بدون پیچیدگی، چیدمان عمودی):**

۱. **انتخاب دپارتمان:** چیپ/کارت‌های درشت و قابل‌لمس در یک گرید. فهرست دپارتمان‌ها در فایل ثابت `src/lib/departments.ts` با `id, name, icon, accentColor` تعریف شود:
- تولید و کارخانه
- فروش و پخش مویرگی
- بازاریابی و برند
- مالی و حسابداری
- منابع انسانی و امور اداری
- فناوری اطلاعات
- فنی و مهندسی و نگهداشت
- لجستیک و زنجیره تأمین
- کنترل کیفیت (QA/QC)
- بهداشت، ایمنی و محیط‌زیست (HSE)
- تحقیق و توسعه (R&D)
- سایر

۲. **عنوان شغلی (یک فیلد کوتاه، اختیاری):** مثلاً «کارشناس فروش حضوری». Placeholder: «مثلاً: سرپرست شیفت تولید — اختیاری». هیچ textarea شرح شغل در معرض کاربر نباشد؛ حداکثر یک لینک کم‌رنگ «توضیح بیشتری داری؟ بنویس (اختیاری)» که یک textarea کوچک باز می‌کند.

۳. **بلوک سوال‌های هوشمند:** بلافاصله بعد از انتخاب دپارتمان (و تایپ عنوان شغلی با debounce ۸۰۰ms)، از endpoint درک‌شغل نسخه جدید سوال‌ها دریافت شود و با اسکلتون لودینگ ظاهر شود. عنوان بلوک: «چی برات مهمه؟ تیک بزن 👇». حداکثر **۸ سوال**، همه با زبان محاوره‌ای ساده، چک‌باکس‌های درشت (کارت‌-چک‌باکس با کل هدف قابل کلیک، حداقل ۴۴px ارتفاع). پیش‌فرض موارد منطقی تیک خورده باشد تا کاربر فقط بخواند و تیک‌های اضافه/برداشته را تنظیم کند. انواع مجاز سوال:
- چک‌باکس بله/خیر (`boolean`) — بخش عمده سوال‌ها
- چندگزینه‌ای تک‌انتخابی به شکل چیپ (`single`) — فقط برای حداقل سابقه: «فرقی نمی‌کند / ۱+ سال / ۳+ سال / ۵+ سال»
- چندانتخابی به شکل چیپ چک‌دار (`multi`) — فقط برای مهارت‌های کلیدی دپارتمان
- هیچ سوال متنی آزاد از کاربر پرسیده نشود.

۴. **دراپ‌زون رزومه‌ها:** همان دراپ‌زون فعلی (کشیدن‌ورهاکردن، چندفایلی، ZIP خودکار باز شود، بدون سقف تعداد، فرمت PDF/DOCX/TXT/ZIP)، با لیست فایل‌ها و امکان حذف تکی/گروهی و جلوگیری از فایل تکراری.

۵. **یک دکمه اصلی بزرگ:** «شروع تحلیل هوشمند ✅» با شمارنده تعداد رزومه. تا دپارتمان انتخاب نشده و حداقل یک رزومه نباشد، غیرفعال باشد. راهنمای زیر دکمه با لحن دوستانه بگو چه چیزی کم است (مثل الگوی فعلی).

۶. پایین همین صفحه: «غربالگری‌های اخیر» — حداکثر ۵ کارت افقی کوچک با دپارتمان، عنوان شغلی، تاریخ جلالی و تعداد سه دسته؛ کلیک روی هرکدام به نتایج همان بچ می‌برد.

**گام ۲ — پردازش:** همان صفحه پردازش فعلی حفظ شود (وضعیت هر فایل: در صف/استخراج/در حال تحلیل/موفق/غیرقابل‌ارزیابی، نوار پیشرفت، نام فردِ در حال بررسی، دکمه لغو). در پایان این گام، نتایج به‌صورت خودکار روی سرور ذخیره شود (بخش ۹).

**گام ۳ — نتایج (مهم‌ترین صفحه — در بخش ۶ و ۷ تفصیل می‌آید).**

---

## بخش ۴ — مشخصات دقیق پرسش‌نامه هوشمند (وظیفه AI)

پاس صفر `understandJob` ارتقا یابد؛ ورودی: `{ departmentId, departmentName, roleTitle, extraNotes? }`؛ خروجی JSON زیر با `responseMimeType: application/json` و temperature معادل ۰.۱:

```json
{
  "department": "فروش و پخش مویرگی",
  "roleTitle": "کارشناس فروش حضوری",
  "seniority": "mid",
  "plainExplanation": "این شغل یه کار میدانی فروشه؛ پس سابقه فروش حضوری، فن بیان و آمادگی ماموریت رو خیلی پررنگ کردم و چون تارگت پورسانی داره، سابقه کار با عدد و تارگت هم مهمه.",
  "thresholds": { "interview": 75, "review": 50 },
  "criteria": [
    { "id": "c1", "title": "سابقه و نتایج فروش میدانی", "weight": 35, "mustHave": true },
    { "id": "c2", "title": "فن بیان، مذاکره و روابط عمومی", "weight": 25, "mustHave": true },
    { "id": "c3", "title": "آشنایی با CRM و گزارش‌گیری", "weight": 15, "mustHave": false },
    { "id": "c4", "title": "آمادگی ماموریت و تردد", "weight": 15, "mustHave": true },
    { "id": "c5", "title": "پایداری و ثبات شغلی", "weight": 10, "mustHave": false }
  ],
  "questions": [
    { "id": "q1", "kind": "knockout", "type": "boolean", "label": "سابقه فروش حضوری یا میدانی داشته باشد", "defaultChecked": true, "weight": 30 },
    { "id": "q2", "kind": "knockout", "type": "boolean", "label": "آماده ماموریت‌های درون‌شهری و جاده‌ای باشد", "defaultChecked": true, "weight": 20 },
    { "id": "q3", "kind": "bonus",    "type": "boolean", "label": "با نرم‌افزار CRM کار کرده باشد", "defaultChecked": false, "weight": 10 },
    { "id": "q4", "kind": "bonus",    "type": "boolean", "label": "سابقه کار پورسانی و تارگت داشته باشد", "defaultChecked": true, "weight": 15 },
    { "id": "q5", "kind": "bonus",    "type": "single",  "label": "حداقل سابقه کار مورد انتظار",
      "options": [ {"value":"any","label":"فرقی نمی‌کند"}, {"value":"1","label":"بالای ۱ سال"}, {"value":"3","label":"بالای ۳ سال"}, {"value":"5","label":"بالای ۵ سال"} ],
      "defaultValue": "1" },
    { "id": "q6", "kind": "bonus", "type": "multi", "label": "کدام مهارت‌ها برایت مهم‌تر است؟",
      "options": [ {"value":"neg","label":"مذاکره و فن بیان"}, {"value":"merch","label":"مرچندایزینگ و چیدمان فروشگاهی"}, {"value":"dist","label":"آشنایی با پخش مویرگی FMCG"}, {"value":"excel","label":"اکسل و گزارش فروش"} ],
      "defaultValues": ["neg","dist"] }
  ]
}
```

قوانین تولید سوال:
- جمع وزن criteriaها دقیقاً ۱۰۰؛ حداقل یک و حداکثر دو معیار `mustHave: true`.
- `knockout` = شرط حذفی؛ اگر کاربر تیکش را زده باشد و در رزومه شواهد خلاف یا فقدان کامل باشد، سقف امتیاز ۴۵ (REVIEW در بهترین حالت). `bonus` = امتیاز مثبت.
- لحن سوال‌ها کاملاً ساده و انسانی باشد (نمونه‌ها: «آماده کار شیفت چرخشی باشد؟»، «سابقه کار کارگاهی/خط تولید داشته باشد؟»، «با سپیدار یا همکاران سیستم کار کرده باشد؟»، «مدرک فنی مرتبط داشته باشد؟»، «نمونه‌کار یا پروژه قابل ارائه داشته باشد؟»).
- آستانه‌ها: مصاحبه بین ۷۰ تا ۸۵ (نقش مدیریتی/حساس نزدیک ۸۵، عمومی نزدیک ۷۰)، بررسی بین ۴۵ تا ۶۰.
- اگر Gemini در دسترس نبود: پاسخ با خطای واضح و دکمه «تلاش مجدد» شکست بخورد؛ **هرگز سوال‌های ثابت و فیک به‌عنوان سوال هوشمند نمایش نده.**

پاسخ کاربر به سرور به شکل `answers: { q1: true, q4: false, q5: "3", q6: ["neg","dist"] }` همراه بچ ذخیره شود و در ارزیابی هر رزومه به پرامپت پاس ۱ تزریق شود.

---

## بخش ۵ — موتور تحلیل هوش مصنوعی (راهنمای AI Director)

### پاس ۱ — ارزیابی هر رزومه

پرامپت فارسی، نقش «کارشناس ارشد غربالگری با وسواس حرفه‌ای». ورودی: درک‌شغل + answers + شرح اختیاری + متن رزومه. خروجی ساختاریافته:

```json
{
  "candidateName": "نام دقیق از متن یا null (هرگز از نام فایل حدس نزن)",
  "contact": { "phone": "09... یا null", "email": "... یا null", "city": "... یا null" },
  "facts": {
    "yearsExperience": 4,
    "education": "کارشناسی مهندسی صنایع یا null",
    "lastRole": "آخرین سمت یا null",
    "skills": ["CRM", "پخش مویرگی", "اکسل"],
    "expectedSalary": "در صورت ذکر در رزومه وگرنه null"
  },
  "criterionScores": [
    { "criterionId": "c1", "score": 82, "rationale": "دو سال و نیم فروش مویرگی در شرکت پخش ...", "evidence": "نقل‌قول مستقیم کوتاه از رزومه" }
  ],
  "score": 78,
  "confidence": "high",
  "recommendation": "INTERVIEW",
  "summary": "۲ تا ۳ جمله روان و صریح: چرا این امتیاز و چرا این دسته.",
  "whyCategory": "یک جمله خیلی ساده برای تیتر دراور (مثلاً: «سابقه فروش میدانی و تارگتش قویه، فقط CRM کار نکرده.») ",
  "strengths": [ { "point": "...", "evidence": "نقل‌قول واقعی" } ],
  "weaknesses": [ { "point": "...", "evidence": "اشاره دقیق به نبود مورد در متن", "severity": "knockout|major|minor" } ],
  "knockoutMisses": ["آمادگی ماموریت در رزومه ذکر نشده"],
  "tags": ["فروش مویرگی", "FMCG", "ویزیتوری"],
  "bankSuggested": false,
  "flags": { "irrelevant": false, "insufficientInfo": false, "scannedNoText": false }
}
```

قوانین نقض‌ناپذیر پرامپت (دقیقاً اعمال و در صورت تخلف مدل، خروجی اصلاح/بازتولید شود):

1. **شاهدمحوری مطلق:** هر نقطه قوت/ضعف و هر امتیاز معیار باید `evidence` با نقل‌قول کوتاه واقعی از رزومه داشته باشد. ادعای بدون شاهد ممنوع.
2. **سقف‌های ضدنمره‌بادکردنی:** رزومه کاملاً نامرتبط → زیر ۳۰ و `irrelevant:true`؛ فقدان هر mustHave یا هر knockout تیک‌خورده → سقف ۴۵؛ رزومه کم‌محتوا → `insufficientInfo:true` بدون امتیازسازی.
3. **سوگیری‌کوری:** نام، جنسیت، سن، وضعیت تأهل، عکس و ملیت در امتیاز بی‌اثر باشند؛ صراحتاً در پرامپت قید شود. فقط شایستگی‌ها سنجیده شوند.
4. **ضدتقلب کلیدواژه‌ای:** صرف فهرست‌کردن مهارت بدون سابقه/پروژه/تجربه، امتیاز کامل ندهد؛ دنبال شواهد تجربه واقعی بگردد. ثبات شغلی (جابه‌جایی‌های پرتکرار کوتاه) به‌عنوان ریسک در weaknesses بیاید.
5. **بازشناسی بافت ایران:** رزومه فارسی و انگلیسی هر دو پشتیبانی شوند؛ معادل‌یابی اصطلاحات (ویزیتور = فروش میدانی، پخش مویرگی، سپیدار/همکاران، HSE و...). پیش از پردازش نرمال‌سازی ی/ک و ارقام فارسی/عربی انجام شود.
6. **بازه‌های متنی:** حداکثر ۷۵۰۰ کاراکتر اول هر رزومه به مدل برسد (الگوی فعلی).
7. **confidence:** اگر شواهد ناقض یا ناکافی بود `low/medium` بدهد تا UI علامت «نیازمند بررسی انسانی» نشان دهد.
8. **برچسب بانک:** اگر کاندید برای شغل فعلی مناسب نیست ولی برای موقعیت‌های آتی همان دپارتمان ارزشمند است `bankSuggested:true` و تگ‌های مناسب بدهد.

### پاس ۲ — کالیبراسیون نسبی (موجود، حفظ و تقویت شود)

بعد از ارزیابی کل بچ: ۱۵ نفر برتر + موارد لبه‌مرزی (±۵ اطراف آستانه‌ها) یکجا به مدل داده شوند تا عدالت نسبی برقرار شود؛ تغییرات حداکثر ±۱۰، clamp بین ۰ تا ۱۰۰، سپس recommendation بر اساس آستانه‌ها دوباره قطعی محاسبه شود. امتیاز نهایی مرجع همان «امتیاز کالیبره‌شده» است.

### تاب‌آوری

- همان مکانیزم فعلی: timeout ۷ ثانیه‌ای برای هر فراخوانی، حداکثر ۲ ریتری با backoff، همزمانی ۲، circuit breaker (۴۲۹ → ۲ دقیقه، ۵۰۳ → ۴۵ ثانیه)، مدل اصلی از `GEMINI_MODEL` با فهرست مدل‌های جایگزین.
- اگر برای یک رزومه پس از همه ریتریها پاسخ AI نیامد: آن رزومه با وضعیت «خطا در تحلیل — دکمه بررسی مجدد» جدا شود؛ در صورت فعال بودن تحلیل محلیِ مبتنی بر کلیدواژه، نتیجه با برچسب صریح «ارزیابی محلی (غیر هوشمند)» و `confidence:"low"` نمایش داده شود تا هیچ‌وقت کاربر فریب نخورد.
- فایل اسکن/تصویری/خراب → دسته چهارم خاکستری «غیرقابل‌ارزیابی» با دلیل شفاف (الگوی `UnjudgeableSection` فعلی حفظ شود)، نه امتیاز صفر در دسته «رد شود».

---

## بخش ۶ — صفحه نتایج

چیدمان از بالا به پایین:

1. **بنر خلاصه:** «از X رزومه، Y نفر برای مصاحبه پیشنهاد می‌شن 🎯» + جستار ساده AI («هوش مصنوعی این شغل رو اینطوری فهمید…» همان `plainExplanation`) + دکمه‌ها: «شروع غربالگری جدید»، «دانلود گزارش اکسل»، «چاپ».
2. **سه تب دسته‌بندی چسبان به بالا (sticky segmented control):**
   - 🟢 **مصاحبه شود** (سبز، بالای آستانه interview) با شمارنده
   - 🟡 **بررسی شود** (کهربایی، بین دو آستانه) با شمارنده
   - 🔴 **رد شود** (قرمز، زیر آستانه review) با شمارنده
   - پیش‌فرض تب «مصاحبه شود» باز باشد؛ اگر خالی بود، اولین تب غیرخالی.
3. **لیست هر دسته:**
   - کارت‌ها بر اساس **اولویت** چیده شوند: امتیاز نهایی نزولی؛ تساوی → تعداد knockoutهای احرازشده بیشتر، سپس سال سابقه بیشتر.
   - رتبه داخل دسته با بج عددی بزرگ (۱ تا ...) روی کارت دیده شود.
   - **حداکثر ۱۰ کارت در هر صفحه؛** اگر بیش از ۱۰ بود، صفحه‌بندی ساده و درشت پایین لیست (قبلی/بعدی + شماره صفحات با ارقام فارسی، RTL، شروع از ۱). صفحه‌بندی هر سه دسته مستقل باشد؛ با تعویض تب، صفحه به ۱ برگردد.
4. **بخش خاکستری «غیرقابل‌ارزیابی»** جمع‌شونده پایین صفحه.

### کارت رزومه (حالت فشرده — پیش‌فرض)

عناصر از راست به چپ در یک کارت تمیز rounded-2xl با سایه خیلی نرم:
- بج رتبه + دایره آواتار با حرف اول نام (رنگ هم‌خانواده دسته)
- نام (یا «کاندید بدون نام — روی فایل بزن»)؛ زیرش یک خط: آخرین سمت • سال سابقه • شهر (هرچه بود)
- ۲ تا ۳ چیپ مهارت کلیدی از `tags`
- **یک خط خلاصه:** همان `whyCategory` با آیکون ربات
- سمت چپ کارت: امتیاز ۰–۱۰۰ با رنگ دسته (عدد بزرگ فارسی) + بج متن دسته
- یک ردیف **۴ دکمه شورتکات با آیکون + تولتیپ فارسی** (همیشه قابل دیدن، روی موبایل تمام‌عرض زیر کارت):

| دکمه | آیکون | رفتار |
|---|---|---|
| **ارسال پیام** | `MessageCircle` | باز کردن مودال پیام (بخش ۷) |
| **بررسی مجدد** | `RefreshCw` | اجرای دوباره تحلیل AI فقط برای همان رزومه با همان معیارها؛ حالت loading روی خود کارت؛ بعد از اتمون، امتیاز/دسته/رتبه به‌صورت زنده به‌روز شود و در تاریخچه ارزیابی ثبت شود. اگر پاسخ سوال‌ها عوض شده باید در بچ جدید باشد؛ این دکمه صرفاً تحلیل تازه با معیار فعلی است. |
| **افزودن به بانک رزومه** | `BookmarkPlus` / بعد از افزودن `BookmarkCheck` | مودال کوچک: دپارتمان مقصد (پیش‌فرض دپارتمان همین غربالگری) + یک یادداشت اختیاری + چیپ تگ‌های پیشنهادی AI قابل ویرایش. بعد از ثبت، دکمه سبز و «در بانک رزومه ✓» شود. |
| **حذف** | `Trash2` | دیالوگ تأیید («این رزومه از نتایج حذف شود؟») سپس حذف نرم (soft delete)؛ اگر در بانک باشد از بانک هم خارج شود و شمارنده‌ها اصلاح شوند. |

- کلیک روی **بدنه کارت** (نه دکمه‌ها) → باز شدن **دراور جزئیات**.

### دراور جزئیات تحلیل (کلیک روی کارت)

دراور کشویی RTL (روی موبایل تمام‌صفحه، روی دسکتاپ عرض ۵۲۰px) شامل:
1. هدر: نام، دسته و امتیاز نهایی، رتبه در دسته، سطح اطمینان AI (قوی/متوسط/ضعیف) و دکمه‌های ۴گانه هم سطح بالا.
2. باکس «چرا این دسته؟»: `whyCategory` + `summary`.
3. **جدول/نوار امتیاز معیارها:** برای هر criterion نوار پیشرفت افقی فارسی با امتیاز، rationale و نقل‌قول.
4. نقاط قوت (حداکثر ۴) با نقل‌قول‌های واقعی در بلوک سبز روشن؛ کمبودها با شدت (حذفی/مهم/جزئی) در بلوک کهربایی/قرمز.
5. موارد حذفی پاس‌نشده (`knockoutMisses`) در صورت وجود، با هشدار قرمز مشخص.
6. کارت اطلاعات استخراج‌شده: تلفن، ایمیل، شهر، سال سابقه، تحصیلات، آخرین سمت، حقوق درخواستی — هر مورد null بود «در رزومه ذکر نشده».
7. دکمه مشاهده/دانلود فایل اصلی رزومه و نام فایل.
8. تاریخچه بررسی مجدد (امتیاز قبلی/جدید و تاریخ جلالی) در صورت وجود.
9. اگر `bankSuggested` بود، بنر ظریف: «هوش مصنوعی پیشنهاد می‌کند برای فرصت‌های آتی در بانک نگه داشته شود» + دکمه افزودن.

---

## بخش ۷ — مودال «ارسال پیام»

- سه قالب آماده با تب: **دعوت به مصاحبه** (برای دسته مصاحبه)، **درخواست تکمیل اطلاعات** (برای دسته بررسی)، **اطلاع نگهداری در بانک** (بعد از افزودن به بانک). متن توسط سرور با Gemini به‌صورت **شخصی‌سازی‌شده** تولید شود: خطاب با نام کاندید، اشاره به یک نقطه قوت واقعی رزومه، لحن مؤدبانه و کوتاه (۴ تا ۶ خط)، با جایگذاری محل/زمان به‌صورت `[زمان]` و `[مکان]` قابل ویرایش در همان مودال. اگر AI در دسترس نبود، قالب ثابت حرفه‌ای با متغیرهای نام/نقش.
- متن داخل textarea قابل ویرایش کامل است.
- دکمه‌ها: **کپی متن** (با حالت «کپی شد ✓»)، **واتساپ** (لینک `https://wa.me/<989xxxxxxxxx>` با نرمال‌سازی شماره ایران؛ اگر تلفن نبود هشدار «در رزومه تلفخی ثبت نشده»)، **پیامک** (لینک `sms:`)، **ایمیل** (`mailto:`). هیچ ارسال واقعی سمت سرور انجام نمی‌شود و هیچ ادعای ارسالی نباشد.
- دکمه تأیید پایین: «به‌عنوان ارسال‌شده علامت بزن» که روی کارت آیکون تیک کوچک «پیام داده‌شده» می‌گذارد و زمان جلالی لاگ می‌شود.

---

## بخش ۸ — تب بانک رزومه

**صفحه ۱ — گرید دپارتمان‌ها (خیلی ساده):**
- کارت برای هر دپارتمان: آیکون، نام، و تعداد رزومه‌های موجود در بانک آن به عدد فارسی. کارت‌های خالی هم دیده شوند با حالت کم‌رنگ و متن «هنوز رزومه‌ای ندارد».
- بالای صفحه یک باکس جستجوی سراسری کوچک (نام/مهارت/تگ) که نتایج را across همه دپارتمان‌ها نشان دهد.

**صفحه ۲ — رزومه‌های یک دپارتمان (با مسیر برگشت واضح):**
- هدر: نام دپارتمان + تعداد کل.
- **نوار فیلترها (ساده، افقی و قابل‌جمع روی موبایل):**
  - جستجوی متنی (نام، آخرین سمت، مهارت، تگ، یادداشت)
  - حداقل امتیاز: چیپ‌های «فرقی نمی‌کند / ۷۰+ / ۸۰+ / ۹۰+»
  - سابقه: چیپ‌های «فرقی نمی‌کند / ۱+ / ۳+ / ۵+ سال»
  - منبع: همه / بچ غربالگری خاص (انتخاب از لیست عنوان شغلی + تاریخ)
  - تگ‌ها: چیپ‌های تگ موجود با چندانتخابی
  - بازه افزودن: همه / این هفته / این ماه (تاریخ جلالی)
  - مرتب‌سازی: جدیدترین افزوده (پیش‌فرض) / بالاترین امتیاز / بیشترین سابقه
  - دکمه «حذف فیلترها» وقتی فیلتری فعال است.
- نتایج همان کارت رزومه استاندارد (با همان ۴ دکمه؛ دکمه «افزودن به بانک» در بانک به «حذف از بانک» تبدیل شود) + کلیک → همان دراور جزئیات (شامل یادداشت‌های بانک و تگ‌ها).
- **حداکثر ۱۰ کارت در هر صفحه با همان صفحه‌بندی فارسی.**
- وضعیت خالی: تصویر/آیکون خالی و یک خط ساده + دکمه «رفتن به غربالگری جدید».

---

## بخش ۹ — مدل داده، API و ماندگاری

### ذخیره‌سازی

بدون دیتابیس. یک استور سمت سرور مشابه الگوی snapshot فعلی در فایل `data/screening-store.json` (در `.gitignore`) با ذخیره debounced بعد از هر تغییر؛ رابط‌های repository تست‌پذیر. در استارت، اگر فایل خراب بود با بکاپ قبلی بوت شود. فایل رزومه اصلی به‌صورت باینری در پوشه `data/resumes/<batchId>/<resumeId>.<ext>` ذخیره شود تا دانلود و تحلیل مجدد ممکن باشد (حداکثر حجم مدیریت شود؛ متن استخراج‌شده هم کنار رکورد ذخیره شود تا فیلتر و جستجو فقط با متن کار کند).

### موجودیت‌ها (TypeScript)

```ts
Department { id; name; icon; accentColor }
ScreeningBatch {
  id; departmentId; departmentName; roleTitle; extraNotes?;
  jobUnderstanding;           // خروجی کامل پاس صفر
  answers;                    // پاسخ‌های چک‌باکسی کاربر
  status: 'processing' | 'done' | 'cancelled';
  stats: { total; interview; review; reject; unjudgeable };
  createdAtJalali; createdAtISO;
}
ResumeRecord {
  id; batchId; departmentId; fileName; filePath; extractedText;
  candidateName; contact; facts; tags;
  criterionScores; score; calibratedScore; confidence;
  recommendation: 'INTERVIEW'|'REVIEW'|'REJECT';
  summary; whyCategory; strengths; weaknesses; knockoutMisses;
  flags: { irrelevant; insufficientInfo; scannedNoText };
  category: 'INTERVIEW'|'REVIEW'|'REJECT'|'UNJUDGEABLE'|'ERROR';
  rankInCategory;
  analysisHistory: [{ score, atJalali, reason: 'initial'|'rerun' }];
  inBank: boolean; bankDepartmentId?; bankNote?; bankTags?; addedToBankAtJalali?;
  messageStatus: 'none'|'sent'; lastMessagedAtJalali?;
  deleted: boolean; createdAtISO;
}
```

### APIها (همه JSON، اعتبارسنجی ورودی، خطاها به فارسی)

- `GET /api/departments`
- `POST /api/screening/understand` بدنه `{departmentId, roleTitle, extraNotes?}` → خروجی پاس صفر + پرسش‌نامه
- `POST /api/screening/batches` → ساخت بچ (درک‌شغل + answers) و برگرداندن `batchId`
- `POST /api/screening/batches/:id/resumes/:resumeId/evaluate` (آپلود multipart یا base64 + متن استخراج‌شده کلاینت؛ سرور تحلیل می‌کند و رکورد را upsert می‌کند؛ پاسخ، رکورد کامل است)
- `POST /api/screening/batches/:id/calibrate` → کالیبراسیون و برگرداندن رکوردهای نهایی با رتبه‌ها
- `GET /api/screening/batches?limit=5` و `GET /api/screening/batches/:id`
- `POST /api/resumes/:id/rerun` (تحلیل مجدد با معیار همان بچ)
- `PATCH /api/resumes/:id` (حذف نرم، علامت پیام، تگ/یادداشت)
- `POST /api/resumes/:id/bank` `{bankDepartmentId, note, tags}` و `DELETE /api/resumes/:id/bank`
- `GET /api/bank/departments` (شامل counts) و `GET /api/bank/departments/:id/resumes?query&minScore&minYears&tag&since&sort&page&pageSize=10`
- `GET /api/resumes/:id/file` (دانلود فایل اصلی)
- `POST /api/messages/draft` `{resumeId, kind}` → متن پیام شخصی‌سازی‌شده
- `GET /api/screening/health` (مثل فعلی؛ وضعیت کلید AI)

سهم‌بندی، تایم‌اوت، ریتری و circuit breaker همان رفتار فعلی حفظ شود.

---

## بخش ۱۰ — زبان و دستورالعمل UI/UX

- **سراسر RTL و فارسی**؛ فونت Vazirmatn؛ همه اعداد فارسی (هیچ رقم لاتین در UI، شامل صفحه‌بندی، امتیاز، تاریخ و اندازه فایل).
- تاریخ‌ها فقط جلالی با ابزار موجود `tehran-time`؛ هیچ تاریخ ثابت/ساختگی.
- رنگ‌بندی معنایی با توکن‌های فعلی: سبز (`#047857`) برای برند و «مصاحبه»، کهربایی (`#b45309`) برای «بررسی»، قرمز (`#b91c1c`) برای «رد/حذف»، خاکستری برای «غیرقابل‌ارزیابی». پس‌زمینه‌ها نرم و چشم‌نواز، کارت‌ها سفید با گردی ۱۴–۱۶px و سایه خیلی ظریف.
- موبایل-فرست و ریسپانسیو (عرض حداقل ۳۶۰px)؛ همه اهداف لمسی حداقل ۴۴×۴۴px؛ هیچ هاورِ ضروری‌ای روی موبایل.
- انیمیشن‌های کوتاه و هدفمند با `motion` (ورود نرم کارت‌ها با stagger کوتاه، حرکت اسکور بین مقادیر، تب‌بندی، اسکلتون‌ها)؛ بدون انیمیشن حواس‌پرت‌کن.
- حالت‌های خالی، لودینگ (اسکلتون به‌جای اسپینر تنها)، و خطا برای هر شبکه/دکمه طراحی شود. خطاها با زبان انسانی و راه حلاع بعدی (مثلاً دکمه «تلاش مجدد»).
- microcopyها محاوره‌ای و مهربان (مثل نمونه‌های فعلی: «اول بگو شغل چیه…»، «هر چند تا رزومه داری همینجا بریز 📥»). هیچ عبارت حقوقی/فنی در معرض کاربر.
- کنتراست متن حداقل WCAG AA (توکن‌های فعلی رعایت شده‌اند؛ حفظ شوند).
- صفحه نتایج و بانک برای چاپ مرتب باشند (`print` styles ساده).

---

## بخش ۱۱ — حالت‌های خاص که باید درست هندل شوند

- ZIP حاوی پوشه‌تو‌پوشه و فایل غیررزومه؛ فایل تکراری (نام+حجم یکسان)؛ پسوند غلط؛ PDF اسکن تصویری؛ رزومه‌های نیمه‌فارسی/انگلیسی؛ رزومه‌ای که نام/تلفن ندارد؛ تلفن با فرمت ۰۹، +۹۸، ۹۸.
- بچ صفر رزومه قابل‌ارزیابی؛ بچ یک رزومه‌ای (کالیبراسیون رد شود)؛ قطع شبکه وسط پردازش؛ لغو پردازش؛ ریتری دکمه‌ای فایل‌های خطاخورده.
- تحلیل مجددی که دسته را عوض کند: رکورد بین تب‌ها جابه‌جا شود، رتبه‌ها و شمارنده‌ها و صفحه‌بندی دوباره محاسبه شوند؛ اگر رکورد الان در صفحه‌ای بالاتر/پایین‌تر رفت، کاربر را با یک toast کوتاه مطلع کن.
- حذف از نتایج و بانک، افزودن تکراری به بانک (جلوگیری با پیام «قبلاً در بانک دپارتمان X است» و پیشنهاد جابه‌جایی دپارتمان).
- همزمانی: صف نتواند بیش از ۲ درخواست همزمان به Gemini بزند؛ درهای مدار رعایت شود.

---

## بخش ۱۲ — معیارهای پذیرش (قبل از تحویل، تک‌تک تست کن)

**جریان اصلی**
- [ ] انتخاب دپارتمان → ظاهر شدن سوال‌های چک‌باکسی هوشمند با اسکلتون، حداکثر ۸ سوال، بدون هیچ فیلد متنی اجباری.
- [ ] آپلود ۵۰+ رزومه (شامل ZIP) بدون سقف؛ پیشرفت لحظه‌ای و لغو کار کند.
- [ ] خروجی نتایج دقیقاً سه تب سبز/کهربایی/قرمز + بخش غیرقابل‌ارزیابی؛ چیدمان هر تب بر اساس اولویت؛ حداکثر ۱۰ کارت در صفحه و صفحه‌بندی فارسی مستقل.
- [ ] هر کارت هر ۴ دکمه را دارد: پیام، بررسی مجدد، افزودن به بانک، حذف؛ و کلیک روی بدنه کارت دراور جزئیات با شواهد نقل‌قول‌شده باز می‌کند.
- [ ] بررسی مجدد فقط همان رزومه را با حالت لودینگ دوباره تحلیل و UI را زنده به‌روز می‌کند و تاریخچه می‌سازد.
- [ ] افزودن به بانک با انتخاب دپارتمان/تگ/یادداشت کار کند و وضعیت دکمه عوض شود.
- [ ] مودال پیام: متن شخصی‌سازی‌شده قابل ویرایش، دکمه کپی، لینک‌های واتساپ/پیامک/ایمیل با نرمال‌سازی شماره، علامت «پیام داده‌شده».
- [ ] تب بانک: گرید دپارتمان‌ها با تعداد؛ ورود به دپارتمان؛ همه فیلترها و مرتب‌سازی و جستجو درست کار کنند؛ صفحه‌بندی ۱۰تایی؛ حذف از بانک.
- [ ] رفرش مرورگر و ری‌استارت سرور: بچ‌ها، نتایج و بانک از `data/screening-store.json` بازگردانده شوند؛ فایل رزومه قابل دانلود باشد.

**کیفیت AI**
- [ ] هیچ امتیازی بدون ملاک و هیچ نقطه قوت/ضعفی بدون نقل‌قول واقعی نیست.
- [ ] رزومه نامرتبط زیر ۳۰ و قرمز؛ فقدان must-have سقف ۴۵؛ فایل اسکن در بخش غیرقابل‌ارزیابی (نه رد).
- [ ] کالیبراسیون لبه‌مرزی اجرا و امتیاز نهایی مبنای رتبه است؛ تغییرات حداکثر ±۱۰.
- [ ] نتایج fallback همیشه برچسب «ارزیابی محلی (غیر هوشمند)» دارند.

**کیفیت فنی/تجربه**
- [ ] هیچ متن/عدد/رزومه/نام ساختگی در هیچ جای اپ نیست؛ همه داده‌ها از ورودی واقعی کاربر است.
- [ ] تمام UI فارسی، RTL، اعداد فارسی، تاریخ جلالی واقعی؛ موبایل و دسکتاپ سالم.
- [ ] `npm run lint` بدون خطا؛ مسیرهای خوشبخت و خطای همه APIها تست شده باشند.
- [ ] هیچ‌کدام از ماژول‌های Out of Scope به کد اضافه نشده باشند.

---

## بخش ۱۳ — ترتیب پیشنهادی اجرا برای خودت

1. پاک‌سازی: حذف لایه HRMS و بریدن `server.ts` تا فقط هسته سلامت/غربالگری بماند؛ تب‌بار دوگانه و روتیگ ساده state‌محور (بدون افزودن react-router مگر لازم شد).
2. فایل ثابت دپارتمان‌ها + گام ۱ (انتخاب دپارتمان، عنوان شغلی، دراپ‌زون).
3. ارتقای پاس صفر با پرسش‌نامه + رندر چک‌باکس/چیپ.
4. استور JSON سرور + ساخت بچ و ذخیره رکوردها حین اجرای صف.
5. ارتقای پاس ۱ به اسکیمای جدید (facts، criterionScores، knockoutMisses، tags، confidence) + کالیبراسیون و رتبه.
6. صفحه نتایج: تب‌های سه‌گانه، صفحه‌بندی، کارت جدید، دراور جزئیات.
7. اکشن‌ها: بررسی مجدد، حذف، افزودن به بانک، مودال پیام و draft سرور.
8. بانک: گرید دپارتمان، فیلترها، صفحه‌بندی.
9. پولیش: انیمیشن، اسکلتون، حالت خالی/خطا، چاپ، ریسپانسیو، تست end-to-end، lint.

**یادت نرود:** این محصول برای یک کارشناس منابع انسانی واقعی است که روزانه ده‌ها رزومه می‌بیند؛ اگر حتی یک مرحله مبهم باشد، محصول شکست خورده است. ساده، مهربان، دقیق.
